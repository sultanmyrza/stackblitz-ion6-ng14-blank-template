import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'stackblitz-ion6-ng14-blank-template',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
